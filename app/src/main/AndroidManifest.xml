<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.launcher.home">

    <!-- 查询已安装应用包名 - 用于获取应用列表 -->
    <!-- 临时注释用于 BlueStacks 测试 - 正式版本需要此权限 -->
    <!-- <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" /> -->
    <!-- 系统级权限 - 无障碍服务需要，用于拦截返回键 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 屏幕保持唤醒 - Kiosk 模式需要 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:launchMode="singleTask">

        <!-- MainActivity - HOME Intent Filter -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WhitelistSettingsActivity - 白名单设置 -->
        <activity
            android:name=".WhitelistSettingsActivity"
            android:label="白名单设置"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <!-- 无障碍服务 - 锁定豆包，拦截返回键 -->
        <service
            android:name=".AccessibilityLockService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
