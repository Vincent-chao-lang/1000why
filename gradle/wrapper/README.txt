Gradle Wrapper JAR

To get the gradle-wrapper.jar file, run this command in the project root:
gradle wrapper --gradle-version 8.2

Or open this project in Android Studio, it will automatically generate the wrapper files.
